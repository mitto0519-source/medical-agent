# ── Core Data Science ────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ── Statistical Analysis ─────────────────────────────────────────────
statsmodels>=0.14.0
pingouin>=0.5.3
lifelines>=0.28.0

# ── Visualization ────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# ── Data Processing ──────────────────────────────────────────────────
openpyxl>=3.1.0

# ── NLP / Embedding ──────────────────────────────────────────────────
# sentence-transformers>=2.2.0  ← 로컬 ChromaDB 전용. Cloud는 Supabase 사용으로 제외

# ── Web / API ────────────────────────────────────────────────────────
streamlit>=1.30.0
# flask>=3.0.0  ← MCP 서버 전용
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.65.0

# ── PDF Processing ───────────────────────────────────────────────────
pymupdf>=1.23.0

# ── Vector DB — local (로컬 개발 시 별도 설치: pip install chromadb) ────
# chromadb>=0.4.0  ← grpcio→googleapis-common-protos→protobuf4.x 충돌로 제외
#                     Streamlit Cloud 에서는 SUPABASE_DB_URL 로 Supabase 사용

# ── Vector DB — cloud (Supabase pgvector, SUPABASE_DB_URL 설정 시 사용)
vecs>=0.4.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
pgvector>=0.3.0
flupy>=1.0.0
deprecated>=1.2.0

# ── LLM ─────────────────────────────────────────────────────────────
anthropic>=0.30.0

# ── MCP Server (Railway 전용 — Streamlit Cloud에서는 불필요) ──────────
# fastmcp>=3.0.0
