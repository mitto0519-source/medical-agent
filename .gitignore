# 환경/인증
.env
*.env.*
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 데이터 (OneDrive로 공유)
data/chromadb/
data/papers/
data/drafts/
data/agent_memory.json
data/medical_knowledge_seed/raw/

# 원시데이터 (용량 크고 재다운로드 가능 + KDCA 정책상 public 게재 금지)
# ★ REVIEW_FIX-11 (2026-06-14): 81fc357 사고 후 전면 차단. raw data는 git 영구 추방.
data/raw/
data/oa_papers/
# ★ 추가: SQLite/대용량 라이브러리 (HF 10MB 한계)
data/library/
data/library/*.db*
**/*.db
**/*.db-shm
**/*.db-wal
# 그래도 명시 안전망 (위에 모두 포함되지만 future drift 방지)
**/*.sav
**/*.sas7bdat
**/*.dta
**/*.zip
**/HN*_ALL*
**/통합본*
**/통합파일*
**/지침서*
# pubmed_refs.json (작은 메타 JSON)만 살림 — 명시적 화이트리스트
!data/raw/knhanes/pubmed_refs.json

# 민감 데이터 (API 키 포함)
data/users.json
data/notebooklm_notebooks.json
data/auto_learn_config.json
data/notebooklm_auth.json

# 라이브러리와 프로필은 동기화 (JSON만 — 지식 자산이라 git 유지)
# data/libraries/ → 동기화 O
# data/author_profiles/ → 동기화 O

# ── 런타임 상태/테스트 산출물 (코드 아님 — 로컬 data 볼륨에 영속, git 노이즈 제외) ──
data/chromadb_test/
data/diagnostics/
data/agent_self/
data/change_log/
data/working_papers/
data/wiki/
data/logs/
# 신규 운영 런타임 SQLite DB (events/tasks/idempotency/lifecycle) — 영속 상태, git 부적합
data/runtime/
# HuggingFace 모델 캐시 (재다운로드 가능, 수십MB)
data/.hf_cache/
# 검증/스크립트 출력 (스크린샷·리포트 — 재생성됨)
scripts/ui_eval_outputs/
scripts/ui_smoke_outputs/
scripts/e2e_outputs/
scripts/screenshots/
scripts/real_session_out/
# 자동학습 산출(주기적 변동, 재생성 가능)
data/knowledge_graph/

# IDE
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db
# MS Office 락 파일 (Word/Excel 열고 있을 때 생성, 닫으면 자동 삭제)
~$*
# auto_sync 데몬 lockfile
.auto_sync.lock

# 임시
*.tmp
*.log

# CodeGraph MCP (외부 코드 탐색 도구 — 로컬 인덱스)
.codegraph/
